function [x y]=randomInCircle(x1,y1,rc)
% RANDOMINCIRCLE Generates a random point in a circular bounding area.
%   Parameters:
%   x - the X coordinate of the circle center
%   y - the Y coordinate of the circle center
%   rc - the radius of the circle

    % a=2*pi*rand;
    % r=sqrt(rand);
    % x=(rc*r)*cos(a)+x1;
    % y=(rc*r)*sin(a)+y1;
end